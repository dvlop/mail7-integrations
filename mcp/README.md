# Moved to its own repository

The Mail7 MCP server now lives at **https://github.com/dvlop/mail7-mcp**.

It was split out so that MCP catalogues (the official registry, Glama, mcp.so)
index the server itself rather than a folder inside this collection.

- npm: [`mail7-mcp`](https://www.npmjs.com/package/mail7-mcp)
- docs: https://mail7.net/integrations/mcp/
